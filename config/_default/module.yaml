# Hugo Modules configuration
# Using local custom theme - no external modules required

mounts:
  - source: layouts
    target: layouts
  - source: assets
    target: assets
  - source: static
    target: static
  - source: content
    target: content
